
<%
imageHelper = (path, name, alt, width = "100", height = "70") => {
    if(name == null) {
        name = 'no-avatar.jpeg'
    }
    return `
    <img src="/${path + name}" alt="${alt}" width="${width}" height="${height}">
    `
}
%>

